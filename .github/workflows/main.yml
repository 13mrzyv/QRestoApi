name: QResto CI Build

on:
  push:
    branches: [ "qresto-dev" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Build the stack
      run: docker-compose build
